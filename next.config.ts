import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Removed static export to enable dynamic API routes and MongoDB integration
};

export default nextConfig;
